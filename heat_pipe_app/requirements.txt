# ---------------------------------------------------------------------------
# Inference-only dependencies for the Heat-Pipe Surrogate web app.
#
# NOTE: TensorFlow, XGBoost, SHAP and pymoo are deliberately ABSENT. The
# production surrogate is a scikit-learn GaussianProcessRegressor, so the app
# needs none of the heavy training-time libraries. This keeps the memory
# footprint comfortably under the ~1 GB Streamlit Community Cloud limit.
#
# scikit-learn is PINNED to 1.7.2 — the exact version that pickled the model
# and scalers. A mismatched version raises InconsistentVersionWarning and can,
# in the worst case, deserialise to subtly wrong predictions. Do not bump it
# without re-exporting the artifacts from the training pipeline.
# ---------------------------------------------------------------------------
streamlit==1.40.2
scikit-learn==1.7.2
numpy==2.1.3
scipy==1.14.1
pandas==2.2.3
plotly==5.24.1
joblib==1.4.2
openpyxl

# Optional: enables loading the saved XGBoost model on the Compare-Models
# page. Moderate footprint; safe on the 1 GB tier. Remove if not needed.
xgboost

# PDF report generation (pure-Python, tiny)
fpdf2
